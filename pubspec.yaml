name: generatec_report_excel
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  form_builder_validators: ^9.1.0
  intl: ^0.18.1
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true
